# angular-consume-restapi
A simple example of Angular that consumes REST API from a remote server to fetch the server time, date, and timezone. The input are converted to UTC+8 via a conversion method from another REST API. 
